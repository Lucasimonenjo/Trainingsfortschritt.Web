name: Deploy Blazor WebAssembly

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore Trainingsfortschritt.Web.csproj

      - name: Publish
        run: dotnet publish Trainingsfortschritt.Web.csproj -c Release -o publish --nologo -p:StaticWebAssetBasePath=/

      - name: Show publish folder structure
        run: |
          echo "=== publish ==="
          find publish -maxdepth 3 -type d -print
          echo "=== publish/wwwroot ==="
          find publish/wwwroot -maxdepth 3 -type d -print
          echo "=== publish/wwwroot/_framework ==="
          find publish/wwwroot/_framework -maxdepth 3 -type f -print || echo "_framework not found"



      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: publish/wwwroot

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
